name: Close superseded package PRs

on:
  schedule:
    - cron: "23 */6 * * *"
  workflow_dispatch:
    inputs:
      apply:
        description: "Close superseded formula and cask PRs. Disable for a dry-run."
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  issues: write
  pull-requests: write

concurrency:
  group: close-superseded-formula-prs
  cancel-in-progress: false

env:
  GH_NO_UPDATE_NOTIFIER: 1
  GH_PROMPT_DISABLED: 1
  GH_REPO: ${{ github.repository }}
  HOMEBREW_NO_AUTO_UPDATE: 1
  HOMEBREW_NO_INSTALL_FROM_API: 1
  HOMEBREW_NO_REQUIRE_TAP_TRUST: 1

jobs:
  close-superseded-prs:
    if: github.repository_owner == 'chenrui333'
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@f8d4222eb633e65c2a0157383cf80861fc7fae7f # main
        with:
          core: true
          cask: false

      - name: Close superseded package PRs
        env:
          APPLY: ${{ github.event_name == 'schedule' || inputs.apply }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          args=()
          if [[ "$APPLY" == "true" ]]; then
            if [[ "$GITHUB_REF_NAME" != "main" ]]; then
              echo "::error ::Refusing to close PRs from non-main ref: $GITHUB_REF_NAME"
              exit 1
            fi
            args+=(--apply)
          fi

          ./cmd/brew-close-superseded-prs "${args[@]}"
